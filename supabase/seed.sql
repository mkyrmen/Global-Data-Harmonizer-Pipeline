-- Local-dev demo seed.
-- The two demo CSV files live in raw_data/ and are uploaded by the backend
-- (scripts/) after signup. This only seeds workspaces for an explicit owner,
-- so nothing hardcoded to a user is inserted here.

select 1;